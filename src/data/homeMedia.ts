/* ==========================================================================
   MÍDIA CRÍTICA DA HOME
   --------------------------------------------------------------------------
   Registro propositalmente pequeno e isolado. A Home não deve importar o
   catálogo inteiro de fotografias/vídeos só para mostrar o poster inicial;
   isso preserva o boot do tablet e mantém o artifact enxuto.
   ========================================================================== */

import ambientVideoPosterThumb from '@/assets/images/thumbs/locanda-video-poster.jpg';

export const HOME_MEDIA = {
  ambientVideoPosterThumb,
} as const;
